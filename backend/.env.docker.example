# ============================================
# Thunder Security Backend - Docker Environment
# ============================================
# Copy to .env.local and fill in your credentials
# Docker Compose will load this file automatically
# ============================================

# ---------------------------------------------
# PostgreSQL Configuration (Local Development)
# ---------------------------------------------
# SECURITY: Change these values for your local environment
# DO NOT commit .env.local to version control
POSTGRES_USER=thunder
POSTGRES_PASSWORD=<your-postgres-password>
POSTGRES_DB=thunder_security

# ---------------------------------------------
# Redis Configuration (Local Development)
# ---------------------------------------------
# SECURITY: Change this value for your local environment
REDIS_PASSWORD=<your-redis-password>

# ---------------------------------------------
# Supabase Configuration (REQUIRED)
# ---------------------------------------------
# Get these from: https://supabase.com/dashboard/project/_/settings/api
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ---------------------------------------------
# Database Configuration (Auto-configured by Docker)
# ---------------------------------------------
# These are set in docker-compose.yml, override if needed
# DATABASE_URL=postgresql://thunder:<your-postgres-password>@postgres:5432/thunder_security?schema=public&connection_limit=5

# ---------------------------------------------
# Redis Configuration (Auto-configured by Docker)
# ---------------------------------------------
# These are set in docker-compose.yml, override if needed
# REDIS_URL=redis://redis:6379
# REDIS_HOST=redis
# REDIS_PORT=6379

# ---------------------------------------------
# Application Configuration
# ---------------------------------------------
# PORT=3000
# NODE_ENV=development
# FRONTEND_URL=http://localhost:5173

# ---------------------------------------------
# Optional: External Supabase Database
# ---------------------------------------------
# If you want to use Supabase PostgreSQL instead of local Postgres,
# uncomment and set your Supabase database URL:
# DATABASE_URL=postgresql://postgres:[password]@db.[project].supabase.co:5432/postgres?schema=public&connection_limit=5
