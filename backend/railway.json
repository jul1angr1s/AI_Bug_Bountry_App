{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy; echo MIGRATE_EXIT=$?; echo NODE_VERSION=$(node --version); echo FILES=$(ls dist/start.js dist/server.js 2>&1); node --max-old-space-size=512 dist/start.js; echo NODE_EXIT=$?",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  },
  "healthcheck": {
    "path": "/api/v1/health",
    "timeout": 300,
    "interval": 30
  },
  "regions": [
    "us-west1"
  ]
}
